experiment:
  name: "n60_dbitnet_1x64_ft_n55"
  output_dir: "output/katan_32"
  # seed: 42

data:
  path: "data/katan_32_n60"

processor:
  swap_pairs: false
  xor_channel: false
  reshape: [1, 64]
  normalize: true

model:
  type: "DBitNet"
  params:
    length: 64
    in_channels: 1
    d1: 256
    d2: 64
    n_filters: 32
    n_add_filters: 16

training:
  epochs: 20
  batch_size: 5000
  num_workers: 24
  eval_interval: 5
  checkpoint_interval: 5
  device: "cuda:0"
  use_amp: true
  pretrained: "output/katan_32/n55_dbitnet_1x64_ft_n50_20260202_122141/checkpoint_10.pth"

  loss:
    type: "BCEWithLogitsLoss"
    params: {}

  optimizer:
    type: "AdamW"
    params:
      lr: 1e-3
      betas: [0.9, 0.999]
      weight_decay: 1e-5